import React from 'react';
import {
    Text,
    View
} from 'react-native';

function MainScreen(props) {
  return <View style={{
    backgroundColor:'red',
    flex:1
  }}>
  </View>;
}

export default MainScreen;